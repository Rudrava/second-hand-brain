import { PageGrid } from "components/PageComp.sc";

const Root = () => {
    return (
        <PageGrid center={true.toString()}>
            <h1>Root</h1>
        </PageGrid>
    );
};

export default Root;
